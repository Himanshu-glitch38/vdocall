# CLIENT SIDE

``` on "```**_matched_**```", (partnerId); ```

``` on "```**_waiting_**```", (); ```

``` on "```**_partner-disconnected_**```", (); ```

``` on "```**_partner-disconnected_**```", (); ```

# SERVER SIDE
### Default
``` on "```**_connection_**```", ({}); ```

``` on "```**_disconnect_**```", ({}); ```

### Custom
``` on "```**_next_**```", ({}); ```

``` on "```**_signal_**```", ({to, data}); ```


